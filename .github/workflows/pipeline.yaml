# name: Deployment pipeline

# on:
#   push:
#     branches:
#       - main

# jobs:
#   simple_deployment_pipeline:    
#     runs-on: ubuntu-20.04   
#     steps:     
#       - uses: actions/checkout@v4
#       - uses: actions/setup-node@v4
#         with:
#           node-version: '20'
#       - name: Install dependencies       
#         run: npm install
#       - name: test
#         env:
#           TEST: ${{ toJson(github.event.commits.*.message)}}
#         run: echo "$TEST"
#       - name: test
#         env:
#           TEST2: ${{ toJson( join( github.event.commits.*.message, ', ') )}}
#         run: echo "$contains($TEST2, '#skip')"
       
      